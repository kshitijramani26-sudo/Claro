# Add your prototype HTML here

Copy your uploaded **`Claro.dc.html`** file into this `docs/design/` folder (next to `design-handoff.md` and `claro-data.js`). It's the pixel-perfect interactive reference. You already have it — just drag it in. Once done, you can delete this note.
